{
    "package": {
        "name": "libMoz2D",
        "repo": "Moz2D",
        "subject": "syrel"
    },

    "version": {
        "name": "1.0"
    },

    "files": [
      { "includePattern": "bin/([a-zA-Z0-9.]+)/([a-zA-Z0-9.]+)/([a-zA-Z0-9]+\\.so)", "uploadPattern": "linux/$1/$2/$3", "matrixParams": { "override": 1 }},
      { "includePattern": "bin/([a-zA-Z0-9.]+)/([a-zA-Z0-9.]+)/([a-zA-Z0-9]+\\.dylib)", "uploadPattern": "osx/$1/$2/$3", "matrixParams": { "override": 1 }},
      { "includePattern": "bin/([a-zA-Z0-9.]+)/([a-zA-Z0-9.]+)/([a-zA-Z0-9]+\\.dll)", "uploadPattern": "win/$1/$2/$3", "matrixParams": { "override": 1 }}
    ],
    "publish": true
}
